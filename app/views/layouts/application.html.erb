<!DOCTYPE html>
<html>
  <head>
    <title>RottnPotatoes</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  </head>
  <body>
    <% if notice.present? %>
      <p><%= notice %></p>
    <% end %>

    <%= yield %>
  </body>
</html>
